; Server ip (will default to "127.0.0.1" if not set)
IP = 10.0.0.57
; Server port (will default to "25565" if not set)
PORT = 5786
; Player name (will default to "Player" if one is not set)
PLAYER_NAME = KaitFreeware