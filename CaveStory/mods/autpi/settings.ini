# Doesn't output an error about "main.lua" missing.
Ignore 'Missing main.lua' error = true

# Writes "arms_level.tbl", "bullet.tbl", and "caret.tbl" to the folder the exe is in.
# Useful if you're a modder and dont want to manually edit the bullet.tbl.
Write Tables on Boot = false